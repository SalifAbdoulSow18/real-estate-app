apiVersion: v1
kind: Service
metadata:
  name: real-estate-app-service
  namespace: real-estate-app
spec:
  selector:
    app: real-estate-app
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30085
  type: NodePort